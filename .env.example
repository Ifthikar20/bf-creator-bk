# ── Environment ──
ENVIRONMENT=development
SECRET_KEY=change-me-in-production

# ── Database (same as betterbliss-auth) ──
DB_NAME=betterbliss
DB_USERNAME=betterbliss_user
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_SSL_MODE=require

# ── AWS (S3 / CloudFront) ──
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
VIDEO_BUCKET_NAME=betterbliss-videos-production
CLOUDFRONT_DOMAIN=d2f89dacgjld08.cloudfront.net

# ── Stripe (Creator payouts) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CONNECT_RETURN_URL=https://creator.betterandbliss.com/earnings

# ── CORS ──
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
